\documentclass{article}

\PassOptionsToPackage{numbers, compress}{natbib}
\usepackage[preprint]{neurips_2025}

\usepackage{ifxetex,ifluatex}
\ifxetex
  \usepackage{fontspec}
  \setmainfont{STIX Two Text}
  \setmonofont{Menlo}
\else\ifluatex
  \usepackage{fontspec}
  \setmainfont{STIX Two Text}
\else
  \usepackage[utf8]{inputenc}
  \usepackage[T1]{fontenc}
  \usepackage{lmodern}
\fi\fi

\usepackage{amsmath, amssymb}
\usepackage{graphicx}
% Cap any inserted image at the text width so figures never overflow.
\setkeys{Gin}{width=\linewidth,keepaspectratio}
\usepackage{longtable, booktabs}
\usepackage{array}
\usepackage{calc}
\usepackage{microtype}
\usepackage{url}
\usepackage[dvipsnames]{xcolor}
\usepackage[
  colorlinks=true,
  citecolor=NavyBlue,
  linkcolor=NavyBlue,
  urlcolor=NavyBlue
]{hyperref}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{fontawesome5}
\usepackage{float}
\floatplacement{figure}{!htbp}

% pandoc helpers
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\pandocbounded}[1]{#1}

% Badge box used after the title (LeWorldModel style, via fcolorbox)
\definecolor{codebg}{RGB}{248,248,248}
\definecolor{codecolor}{RGB}{140,142,144}
\newcommand{\linkbadge}[3]{%
  \href{#1}{\colorbox{codebg}{\textcolor{codecolor}{\small\,#2\enspace #3\,}}}%
}

\title{$title$}

$if(author)$
\author{%
$author$\textsuperscript{1}\\[6pt]
\textsuperscript{1}Independent Researcher
}
$endif$

$if(date)$
\date{$date$}
$endif$

\begin{document}

\maketitle

\vspace{-2.0em}
\begin{center}
\linkbadge{https://github.com/keon/world-model-for-coding}{\faGithub}{Code \& Survey}\,%
\linkbadge{https://arxiv.org/}{\faFilePdf}{arXiv}
\end{center}
\vspace{4pt}

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{fig_timeline.pdf}
  \caption{\small \textbf{Twelve-year arc of code world models.} Three parallel lineages --- neural execution, world models / RL, and code LLMs --- converge in 2025--2026 around the named artifact CWM. A single diamond ($$\blacklozenge$$) marks the first paper to use ``world model'' / ``Dreamer'' in a contribution name; a double diamond marks CWM as the named open-weights artifact.}
  \label{fig:timeline}
\end{figure}

$body$

\bibliographystyle{unsrtnat}
\bibliography{references}

\end{document}
